#version 410

//======================================================================
//  Particle.frag
//  ・単純なテクスチャ描画のみ（加算合成などはOpenGL側で）
//  ・座標はパーティクルスプライトのワールド位置（フォグなし）
//======================================================================


//======================================================================
//  Inputs from Vertex Shader
//======================================================================

// UV座標（テクスチャサンプリング用）
in vec2 fragTexCoord;

// パーティクルのワールド座標
in vec3 fragWorldPos;


//======================================================================
//  Outputs
//======================================================================

// 出力カラー
out vec4 outColor;


//======================================================================
//  Uniforms
//======================================================================

// パーティクルテクスチャ
uniform sampler2D uTexture;

// カメラ位置（距離フェードなどで使う可能性がある）
// ※現状では未使用だが、拡張余地として残してある
uniform vec3 uCameraPos;


//======================================================================
//  main()
//======================================================================
void main()
{
    //------------------------------------------------------------------
    // Step 1 : テクスチャカラーをそのまま出力
    //          ※アルファはテクスチャ側に依存
    //          ※ブレンドはOpenGL側で設定（加算/アルファブレンド等）
    //------------------------------------------------------------------
    outColor = texture(uTexture, fragTexCoord);
}
