#version 410 core

//==================================================
// WeatherScreen.vert
// 前景エフェクト（雨・雪・フォグなど）のフルスクリーン描画用。
// 画面に貼るだけの四角形（-1〜1 の2D頂点）をそのまま出力し、
// UV を [0,1] に変換してフラグメントシェーダに渡す。
//==================================================

//------------------------------
// Attributes
//------------------------------
layout (location = 0) in vec2 aPos;  // フルスクリーン用の頂点(-1〜1の範囲)

//------------------------------
// Varyings
//------------------------------
out vec2 vUV; // [0,1] の UV 座標をフラグメントへ送る

//==================================================
// メイン
//==================================================
void main()
{
    // aPos: [-1, +1] なので、UV にするため 0.5 を掛けてオフセット加算
    //
    //   -1 → 0
    //    0 → 0.5
    //   +1 → 1
    //
    vUV = aPos * 0.5 + 0.5;

    // フルスクリーン四角形なので World/ViewProj 変換は不要
    gl_Position = vec4(aPos, 0.0, 1.0);
}
